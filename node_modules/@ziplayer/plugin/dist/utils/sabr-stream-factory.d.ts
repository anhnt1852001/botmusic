import { Readable } from "stream";
import type Innertube from "youtubei.js";
export interface OutputStream {
    stream: NodeJS.WritableStream;
    filePath: string;
}
export interface SabrAudioResult {
    title: string;
    stream: Readable;
    format: {
        mimeType: string;
        itag: number;
        contentLength: number;
    };
}
export interface SabrPlaybackOptions {
    preferWebM?: boolean;
    preferOpus?: boolean;
    videoQuality?: string;
    audioQuality?: string;
    enabledTrackTypes?: any;
}
/**
 * Creates a SABR audio stream for YouTube video download
 * This provides better quality and more reliable streaming than standard methods
 */
export declare function createSabrStream(videoId: string, innertube: Innertube, options?: SabrPlaybackOptions): Promise<SabrAudioResult>;
/**
 * Creates an output stream for writing downloaded content
 */
export declare function createOutputStream(videoTitle: string, mimeType: string): OutputStream;
/**
 * Sanitizes a filename by removing invalid characters
 */
export declare function sanitizeFileName(name: string): string;
/**
 * Converts bytes to megabytes
 */
export declare function bytesToMB(bytes: number): string;
/**
 * Default sabr playback options - optimized for memory usage
 * Using MEDIUM quality and WebM/Opus reduces bandwidth by ~30-40%
 */
export declare const DEFAULT_SABR_OPTIONS: SabrPlaybackOptions;
//# sourceMappingURL=sabr-stream-factory.d.ts.map